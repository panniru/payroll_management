= title("#{@employee_master.present? ? @employee_master.name : ""} Payslip")
.col-md-12
  .pull-right
    = image_tag "logo.jpg", :align => 'left', :class => "img-rounded", :style => "margin-left:33px;" 
.col-md-12
  = render "payslip_header"
.col-md-12
  = render "payslip_header_2"
.col-md-12
  = render :partial => "form", :locals => {:url => employee_master_payslip_path(@employee_master, @payslip)}
